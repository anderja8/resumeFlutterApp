import 'package:flutter/material.dart';

class CallBackGuessPage extends StatelessWidget {
  CallBackGuessPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Business Card Page');
  }
}
